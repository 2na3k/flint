# `Flint`: not Flink, but my own implementation on data pipeline framework

(Based on smallpond on https://github.com/deepseek-ai/smallpond)

Using `duckdb` and `pyarrow` backend to create lightweight distributed data pipeline, onto file system (and cloud).

It's must be lazy, not like pandas.
